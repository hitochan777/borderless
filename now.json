{
  "version": 2,
  "builds": [
    {
      "src": "server/index.ts",
      "use": "@now/node",
      "config": { "maxLambdaSize": "15mb" }
    },
    { "src": "client/package.json", "use": "@now/next" }
  ],
  "routes": [{ "src": "/graphql", "dest": "server/index.ts" }]
}
