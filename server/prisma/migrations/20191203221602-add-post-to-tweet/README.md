# Migration `20191203221602-add-post-to-tweet`

This migration has been generated by Otsuki Hitoshi at 12/3/2019, 10:16:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Tweet" (
  "content" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "inReplyTo" TEXT NOT NULL   REFERENCES "Repliable"(id) ON DELETE RESTRICT,
  "post" TEXT NOT NULL   REFERENCES "Post"(id) ON DELETE RESTRICT,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Tweet" ("content","createdAt","id","inReplyTo","updatedAt","user") SELECT "content","createdAt","id","inReplyTo","updatedAt","user" from "Tweet"

DROP TABLE "lift"."Tweet";

ALTER TABLE "lift"."new_Tweet" RENAME TO "Tweet";

CREATE UNIQUE INDEX "lift"."Tweet.id" ON "Tweet"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191202214625-initial..20191203221602-add-post-to-tweet
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
@@ -34,9 +34,9 @@
   id        String  @default(cuid()) @id @unique
   content   String
   inReplyTo Repliable
   user      User
-  text      String
+  post      Post
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20191203221602-add-post-to-tweet)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20191203221602-add-post-to-tweet";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
