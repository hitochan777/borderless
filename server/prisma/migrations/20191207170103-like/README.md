# Migration `20191207170103-like`

This migration has been generated by Otsuki Hitoshi at 12/7/2019, 5:01:03 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."_RepliableToUser" (
  "A" TEXT NOT NULL   REFERENCES "Repliable"(id) ON DELETE CASCADE,
  "B" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "quaint"."_RepliableToUser_AB_unique" ON "_RepliableToUser"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191207154159..20191207170103-like
--- datamodel.dml
+++ datamodel.dml
@@ -1,15 +1,16 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
 }
 model Repliable {
   id String @default(cuid()) @id @unique
+  likedBy User[]
 }
 model User {
   id                String  @default(cuid()) @id @unique
@@ -19,8 +20,9 @@
   fluentLanguages   String?
   learningLanguages String?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
+  likes Repliable[]
 }
 model Post {
   id        String  @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20191207170103-like)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20191207170103-like";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
