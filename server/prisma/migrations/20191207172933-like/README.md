# Migration `20191207172933-like`

This migration has been generated by Otsuki Hitoshi at 12/7/2019, 5:29:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "quaint"."_RepliableToUser_AB_unique"

DROP TABLE "quaint"."_RepliableToUser";

CREATE TABLE "quaint"."Like" (
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "repliable" TEXT NOT NULL   REFERENCES "Repliable"(id) ON DELETE RESTRICT,
  "user" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "quaint"."Like.id" ON "Like"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191207170103-like..20191207172933-like
--- datamodel.dml
+++ datamodel.dml
@@ -1,18 +1,24 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:dev.db"
 }
 generator photon {
   provider = "photonjs"
 }
 model Repliable {
   id String @default(cuid()) @id @unique
-  likedBy User[]
 }
+model Like {
+  id String @default(cuid()) @id @unique
+  user User
+  repliable Repliable
+  createdAt DateTime  @default(now())
+}
+
 model User {
   id                String  @default(cuid()) @id @unique
   uid               String  @unique
   email             String  @unique
@@ -20,9 +26,8 @@
   fluentLanguages   String?
   learningLanguages String?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
-  likes Repliable[]
 }
 model Post {
   id        String  @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20191207172933-like)
in your `before` or `after` migration script like this:

```ts
import Photon from "@generated/photon/20191207172933-like";

const photon = new Photon();

async function main() {
  const result = await photon.users();
  console.dir(result, { depth: null });
}

main();
```
