# Migration `20191230195228`

This migration has been generated by Otsuki Hitoshi at 12/30/2019, 7:52:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Post" (
  "content" TEXT NOT NULL DEFAULT ''  ,
  "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "id" TEXT NOT NULL   ,
  "language" TEXT NOT NULL DEFAULT ''  ,
  "published" BOOLEAN NOT NULL DEFAULT false  ,
  "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00'  ,
  "user" TEXT NOT NULL   REFERENCES "User"(id) ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Post" ("content","createdAt","id","language","published","updatedAt","user") SELECT "content","createdAt","id","language","published","updatedAt","user" from "Post"

DROP TABLE "quaint"."Post";

ALTER TABLE "quaint"."new_Post" RENAME TO "Post";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

CREATE UNIQUE INDEX "quaint"."User.username" ON "User"("username")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20191222205820..20191230195228
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,16 @@
 datasource development {
   provider = "sqlite"
-  url = "***"
+  url      = env("DEVELOPMENT_DB_URL")
+  enabled  = env("DEVELOPMENT_DB_URL")
 }
+datasource production {
+  provider = "mysql"
+  url      = env("PRODUCTION_DB_URL")
+  enabled  = env("PRODUCTION_DB_URL")
+}
+
 generator photon {
   provider = "photonjs"
 }
@@ -20,9 +27,9 @@
 model User {
   id                String  @default(cuid()) @id
   email             String  @unique
-  username          String
+  username          String  @unique
   fluentLanguages   String?
   learningLanguages String?
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
@@ -30,9 +37,9 @@
 model Post {
   id        String  @default(cuid()) @id
   published Boolean
-  language  Int
+  language  String
   content   String
   user      User
   createdAt DateTime  @default(now())
   updatedAt DateTime  @updatedAt
```
