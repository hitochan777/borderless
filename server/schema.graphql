### This file was autogenerated by Nexus 0.11.7
### Do not make changes to this file directly


type AuthData {
  token: String!
}

type Language implements Node {
  """Unique identifier for the resource"""
  id: ID
  name: String!
}

type Line implements Node {
  """Unique identifier for the resource"""
  id: ID
  partialLines: [PartialLine!]!
  replies: [Tweet!]!
}

input LineInput {
  id: Int
  partialLines: [PartialLineInput!]!
}

type Mutation {
  logout: Boolean!
  postCreate(post: PostInput!): Post!
  postUpdate(id: Int!, post: PostInput!): Post!
  signin(token: String!): AuthData!
  tweetCreate(tweet: TweetInput!): Tweet!
  userCreate(id: String!): User!
  userUpdate(id: String!, user: UserInput!): User!
}

interface Node {
  """Unique identifier for the resource"""
  id: ID
}

type PartialLine {
  text: String!
}

input PartialLineInput {
  referes: [String!]
  subtext: String!
}

type Post implements Node {
  """Unique identifier for the resource"""
  id: ID
  isDraft: Boolean!
  json: String!
  language: Language!
  lines: [Line!]!
  replies: [Tweet!]!
  title: String!
  user: User!
}

input PostInput {
  isDraft: Boolean! = true
  language: Int!
  lines: [LineInput!]!
}

type Query {
  feed(uid: String!): [Post!]!
  langs: [Language!]!
  post(id: Int!): Post!
  posts: [Post!]!
  tweet(
    """tweet id"""
    id: Int!
  ): Tweet!
  viewer: User!
}

"""Anything that can be replied"""
union Repliable = Line | Tweet

type Tweet implements Node {
  """Unique identifier for the resource"""
  id: ID
  inReplyTo: Int
  post: Post!
  postedBy: User!
  replies: [Tweet!]!
  text: String!
}

input TweetInput {
  inReplyTo: Int!
  postId: Int!
  text: String!
}

type User implements Node {
  email: String!
  fluentLanguages: [Int!]!

  """Unique identifier for the resource"""
  id: ID
  learningLanguages: [Int!]!
  posts: [Post!]!
  username: String!
}

input UserInput {
  email: String!
  fluentLanguages: [Int!]!
  learningLanguages: [Int!]!
  username: String!
}
