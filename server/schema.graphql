### This file was autogenerated by Nexus 0.11.7
### Do not make changes to this file directly


type AuthData {
  token: String!
}

type Language implements Node {
  """Unique identifier for the resource"""
  id: ID!
  name: String!
}

type Line implements Node & Repliable {
  """Unique identifier for the resource"""
  id: ID!
  post: Post!
  text: String!
  tweets: [Tweet!]!
}

type Mutation {
  logout: Boolean!
  postCreate(post: PostInput!): Post!
  signin(token: String!): AuthData!
  userCreate(id: String!): User!
  userUpdate(id: String!, user: UserInput!): User!
}

interface Node {
  """Unique identifier for the resource"""
  id: ID!
}

type Post implements Node {
  """Unique identifier for the resource"""
  id: ID!
  language: Language!
  text: String!
  user: User!
}

input PostInput {
  language: Int!
  text: String!
}

type Query {
  feed(uid: String!): [Post!]!
  langs: [Language!]!
  post(id: Int!): Post!
  posts: [Post!]!
  viewer: User!
}

interface Repliable {
  text: String!
}

type Tweet implements Node & Repliable {
  """Unique identifier for the resource"""
  id: ID!
  inReplyTo: Repliable!
  replies: [Tweet!]!
  text: String!
}

type User implements Node {
  email: String!
  fluentLanguages: [Int!]!

  """Unique identifier for the resource"""
  id: ID!
  learningLanguages: [Int!]!
  posts: [Post!]!
  username: String!
}

input UserInput {
  email: String!
  fluentLanguages: [Int!]!
  learningLanguages: [Int!]!
  username: String!
}
